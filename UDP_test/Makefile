# Makefile generated by the project manager
#


## core information

PRJ_FILE       = UDP_test.wpj
PRJ_DIR        = C:/Tornado/target/proj/UDP_test
PRJ_TYPE       = vxApp
PRJ_OBJS       = UDP_test.o
BUILD_SPEC     = PPC860gnu
TGT_DIR        = $(WIND_BASE)/target




## build-configuration info

ifeq ($(BUILD_SPEC),PPC860gnu)
CPU              = PPC860
TOOL             = gnu
DEFAULT_RULE     = UDP_test.out
endif

include $(TGT_DIR)/h/make/defs.project

## build-configuration info

ifeq ($(BUILD_SPEC),PPC860gnu)
AR               = arppc
AS               = ccppc
CC               = ccppc
CFLAGS           = -g -mcpu=860 -ansi -nostdinc -DRW_MULTI_THREAD -D_REENTRANT -fvolatile -fno-builtin -fno-for-scope -I. -IC:/Tornado/target/h -DCPU=PPC860
CFLAGS_AS        = -g -mcpu=860 -ansi -nostdinc -fvolatile -fno-builtin -fno-for-scope -P -x assembler-with-cpp -I. -IC:/Tornado/target/h -DCPU=PPC860
CPP              = ccppc -E -P -xc
LD               = ldppc
LDFLAGS          = -X -N
LD_PARTIAL_FLAGS = -X -r
NM               = nmppc
OPTION_DEFINE_MACRO = -D
OPTION_INCLUDE_DIR = -I
SIZE             = sizeppc
POST_BUILD_RULE  = 
PRJ_LIBS         = 
endif

# override make definitions only below this line


# override make definitions only above this line
include $(TGT_DIR)/h/make/rules.project



## build-configuration info

ifeq ($(BUILD_SPEC),PPC860gnu)
UDP_test.o:
	$(CC) -g -mcpu=860 -ansi -nostdinc -DRW_MULTI_THREAD -D_REENTRANT -fvolatile -fno-builtin -fno-for-scope -I. -I$(WIND_BASE)/target/h -DCPU=PPC860 -c $(PRJ_DIR)/UDP_test.c
endif


## dependencies

UDP_test.o: $(PRJ_DIR)/UDP_test.c


## user defined rules

