# Makefile generated by the project manager
#


## core information

PRJ_FILE       = cgem_vxworks_multifiles.wpj
PRJ_DIR        = C:/Tornado/target/proj/cgem_vxworks_multifiles
PRJ_TYPE       = vxApp
PRJ_OBJS       = sd_cgem_cfg.o sd_cgem.o
BUILD_SPEC     = PPC604gnu
TGT_DIR        = $(WIND_BASE)/target




## build-configuration info

ifeq ($(BUILD_SPEC),PPC604gnu)
CPU              = PPC604
TOOL             = gnu
DEFAULT_RULE     = cgem_vxworks_multifiles.out
endif

include $(TGT_DIR)/h/make/defs.project

## build-configuration info

ifeq ($(BUILD_SPEC),PPC604gnu)
AR               = arppc
AS               = ccppc
CC               = ccppc
CFLAGS           = -g -mstrict-align -ansi -nostdinc -DRW_MULTI_THREAD -D_REENTRANT -fvolatile -fno-builtin -fno-for-scope -I. -IC:/Tornado/target/h -DCPU=PPC604
CFLAGS_AS        = -g -mstrict-align -ansi -nostdinc -fvolatile -fno-builtin -fno-for-scope -P -x assembler-with-cpp -I. -IC:/Tornado/target/h -DCPU=PPC604
CPP              = ccppc -E -P -xc
LD               = ldppc
LDFLAGS          = -X -N
LD_PARTIAL_FLAGS = -X -r
NM               = nmppc
OPTION_DEFINE_MACRO = -D
OPTION_INCLUDE_DIR = -I
SIZE             = sizeppc
POST_BUILD_RULE  = 
PRJ_LIBS         = 
endif

# override make definitions only below this line


# override make definitions only above this line
include $(TGT_DIR)/h/make/rules.project



## build-configuration info

ifeq ($(BUILD_SPEC),PPC604gnu)
sd_cgem_cfg.o:
	$(CC) -g -mstrict-align -ansi -nostdinc -DRW_MULTI_THREAD -D_REENTRANT -fvolatile -fno-builtin -fno-for-scope -I. -I$(WIND_BASE)/target/h -DCPU=PPC604 -c $(PRJ_DIR)/sd_cgem_cfg.c
sd_cgem.o:
	$(CC) -g -mstrict-align -ansi -nostdinc -DRW_MULTI_THREAD -D_REENTRANT -fvolatile -fno-builtin -fno-for-scope -I. -I$(WIND_BASE)/target/h -DCPU=PPC604 -c $(PRJ_DIR)/sd_cgem.c
endif


## dependencies

sd_cgem_cfg.o: $(PRJ_DIR)/sd_cgem_cfg.c $(PRJ_DIR)/sd_cgem.h $(PRJ_DIR)/sd_cgem_cfg.h

sd_cgem.o: $(PRJ_DIR)/sd_cgem.c $(PRJ_DIR)/sd_cgem.h


## user defined rules

